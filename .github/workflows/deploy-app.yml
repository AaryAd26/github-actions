# name: Deploy flask app

# on:
#     workflow_dispatch:

# jobs:
#     deploy:
#         runs-on: self-hosted
        
#     steps:
#         # - name: name checkout code
#           uses: action/checkout@v4

#         - name: deploy with docker compose
#           uses:  